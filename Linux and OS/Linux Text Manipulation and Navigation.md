**Linux and Text Manipulation and Navigation**

- *Standard Output*
  - The *>* Operator
    This operator is used to redirect the output to a text file, if that file does not exist, it will be created
```
echo "|text|" > |filename|
```
